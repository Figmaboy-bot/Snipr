/**
 * Copy this file to `firebase-config.js` in the project root and fill in your
 * Firebase project values (Project settings → General → Your apps).
 *
 * Enable Authentication: Email/Password + Google in Firebase Console.
 *
 * For Google sign-in from the extension, create an OAuth client of type
 * "Chrome Extension" in Google Cloud Console and put its client ID in
 * `manifest.json` under `oauth2.client_id`.
 */
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:xxxxxxxxxxxxxxxx",
};
